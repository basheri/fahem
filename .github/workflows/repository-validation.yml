name: Repository validation

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  validate-kit:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate repository kit
        run: ./scripts/validate-kit.sh
